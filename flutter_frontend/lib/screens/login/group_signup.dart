import 'package:flutter/material.dart';

class GroupSignupPage extends StatelessWidget {
  const GroupSignupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('cup')),
      body: const Center(child: Text('絕命測試中...')),
    );
  }
}
